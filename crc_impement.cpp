// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;

bool generatingPc(bool *ar,bool *ar){
    
    
}

int main() {
    int msglen,genlen;
    cout<<"Enter the Length of tye message : ";cin>>msglen;
    cout<<"Enter the length of the Generating function : ";cin>>genlen;
    bool msg[msglen],gen[genlen];
    cout<<"\nEntetr the mesasge by giving spaces in b/w in each bit:\n";
    for(int i =0;i<msglen;i++){
        cin>> msg[i];
    }
    cout<<"\nEnter the Generator bits by giving spaces in b/w in each bit:\n";
    for(int i =0;i<genlen;i++){
        cin>> gen[i];
    }
    if(gen[0]==1 && gen[genlen-1]==1){
        
    }
    
    
    
    
    
    
    

    return 0;
}